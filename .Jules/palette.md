## 2024-05-18 - Missing ARIA Labels on Close Buttons
**Learning:** Icon-only close and dismiss buttons frequently miss `aria-label` attributes, hindering accessibility for screen reader users.
**Action:** Add `aria-label` with clear actions (e.g., "Close modal", "Dismiss action") to any interactive element that relies solely on an icon for its visual affordance.
